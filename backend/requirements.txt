Terminal 1 — Backend
cd /d/document-converter/backend
python -m uvicorn app.main:app --reload

Run:
python -m uvicorn app.main:app --reload

Terminal 2 — Frontend

Open a new Git Bash terminal.
cd /d/document-converter/frontend
npm run dev



…or create a new repository on the command line
echo "# document-converter" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/nithinnaidun/document-converter.git
git push -u origin main




…or push an existing repository from the command line
git remote add origin https://github.com/nithinnaidun/document-converter.git
git branch -M main
git push -u origin main